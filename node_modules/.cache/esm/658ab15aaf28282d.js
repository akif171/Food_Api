let Product,multer,path,Joi,fs,CustomErrorHandler,productSchema;_af2‍.x([["default",()=>_af2‍.o]]);_af2‍.w("../models",[["Product",["Product"],function(v){Product=v}]]);_af2‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_af2‍.w("path",[["default",["path"],function(v){path=v}]]);_af2‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_af2‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_af2‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_af2‍.w("../validators/productValidator",[["default",["productSchema"],function(v){productSchema=v}]]);







const storage = multer.diskStorage({
  destination: (req, file, cb) => cb(null, "uploads/"),
  filename: (req, file, cb) => {
    const uniqueName = `${Date.now()}-${Math.round(
      Math.random() * 1e9
    )}${path.extname(file.originalname)}`;

    cb(null, uniqueName);
  },
});

const handleMultiPartData = multer({
  storage,
  limits: { fileSize: 1000000 * 5 },
}).single("image");

const productController = {
  async store(req, res, next) {
    //multipart form data
    handleMultiPartData(req, res, async (err) => {
      if (err) {
        return next(CustomErrorHandler.serverError(err.message));
      }
      _af2‍.g.console.log(err);

      const filePath = req.file.path;

      //validation

      const { error } = productSchema.validate(req.body);

      if (error) {
        //Delete the uploaded file

        fs.unlink(`${appRoot}/${filePath}`, (err) => {
          if (err) {
            return next(CustomErrorHandler.serverError(err.message));
          }
        });
        _af2‍.g.console.log(error);
        return next(error);
      }

      const { name, price, size } = req.body;

      let document;

      try {
        document = await Product.create({
          name,
          price,
          size,
          image: filePath,
        });
      } catch (error) {
        _af2‍.g.console.log(error);
        return next(error);
      }

      res.json(document);
    });
  },
  update(req, res, next) {
    handleMultiPartData(req, res, async (err) => {
      if (err) {
        return next(CustomErrorHandler.serverError(err.message));
      }
      _af2‍.g.console.log(err);

      let filePath;
      if (req.file) {
        filePath = req.file.path;
      }

      //validation

      const { error } = productSchema.validate(req.body);

      if (error) {
        //Delete the uploaded file

        if (req.file) {
          fs.unlink(`${appRoot}/${filePath}`, (err) => {
            if (err) {
              return next(CustomErrorHandler.serverError(err.message));
            }
          });
        }
        _af2‍.g.console.log(error);
        return next(error);
      }

      const { name, price, size } = req.body;

      let document;

      try {
        document = await Product.findOneAndUpdate(
          { _id: req.params.id },
          {
            name,
            price,
            size,
            ...(req.file && { image: filePath }),
          },
          {
            new: true,
          }
        );
      } catch (error) {
        _af2‍.g.console.log(error);
        return next(error);
      }

      _af2‍.g.console.log(req.body.id);
      res.json(document);
    });
  },
  async delete(req, res, next) {
    const document = await Product.findByIdAndRemove({ _id: req.params.id });

    if (!document) {
      return next(new Error("Nothing to delete"));
    }

    //image delete
    const imagePath = document.image;
    fs.unlink(`${appRoot}/${imagePath}`, (err) => {
      if (err) {
        return next(CustomErrorHandler.serverError());
      }
    });

    res.json(document);
  },
  async index(req, res, next) {
    let document;
    try {
      //pagination (mongoose pagination)

      document = await Product.find()
        .select("-updatedAt -__v")
        .sort({ createdAt: -1 });
    } catch (error) {
      _af2‍.g.console.log(error);
      return next(CustomErrorHandler.serverError());
    }
    res.json(document);
  },

  async show(req, res, next) {
    let document;
    try {
      document = await Product.findOne({ _id: req.params.id }).select(
        "-updatedAt -__v"
      );
    } catch (error) {
      _af2‍.g.console.log(error);
      return next(CustomErrorHandler.serverError());
    }

    return res.json(document);
  },
};

_af2‍.d(productController);
