let mongoose,APP_URL;_659‍.x([["default",()=>_659‍.o]]);_659‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_659‍.w("../config",[["APP_URL",["APP_URL"],function(v){APP_URL=v}]]);

const Schema = mongoose.Schema;

const productSchema = new Schema(
  {
    name: {
      type: String,
      required: true,
    },
    price: {
      type: String,
      required: true,
    },
    size: {
      type: String,
      required: true,
    },
    image: {
      type: String,
      required: true,
      get: (image) => {
        return `${APP_URL}/${image}`;
      },
    },
  },
  { timestamps: true, toJSON: { getters: true },id }
);

_659‍.d(mongoose.model("Product", productSchema, "products"));
